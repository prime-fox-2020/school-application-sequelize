<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">School App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= title == 'Home' ? 'active' : ''%>">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item" <%= title == 'Teachers' ? 'active' : ''%>>
                <a class="nav-link" href="/teachers">Teachers</a>
            </li>
            <li class="nav-item" <%= title == 'Students' ? 'active' : ''%>>
                <a class="nav-link" href="/students">Students</a>
            </li>
            <li class="nav-item" <%= title == 'Subjects' ? 'active' : ''%>>
                <a class="nav-link" href="/subjects">Subjects</a>
            </li>
        </ul>
        <% if(title !== 'Home' && title !== 'Error') {%>
        <form class="form-inline my-2 my-lg-0">
            <a href="/<%= title.toLowerCase()%>/add" class="btn btn-outline-success my-2 my-sm-0" type="submit">Add
                Data</a>
        </form>
        <%}%>
    </div>
</nav>

<%if(title == 'Home' || title == 'Error') {%>
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h1 class="display-4"><%= text%></h1>
            </div>
            <%if(title == 'Error') {%>
            <div class="container">
                <a class="btn btn-primary btn-lg" href="/" role="button">Back to home</a>
            </div>
            <%}%>
  </div>
  <%}%>

            </body>

            </html>