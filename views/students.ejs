<!DOCTYPE html>
<html lang="en">
<%- include ('./partials/head', {title: "Students"}) %>

<body>
    <%- include ('./partials/navbar') %>
    <div class="jumbotron jumbotrom-fluid bg-light">
        <div class="container">
            <h1 class="display-3">List Students</h1>
            <p class="lead">
                <a class="btn btn-primary" href="/students/add" role="button">Add Students</a>
            </p>

            <% if (msg) { %>
            <div class="alert alert-info" role="alert">
                <%= msg %>
            </div>
            <% } %>

            <% if (data) { %>
            <table class="table table-striped table-inverse table-responsive">
                <thead class="thead-inverse">
                    <tr>
                        <th>id</th>
                        <th>first name</th>
                        <th>last name</th>
                        <th>email</th>
                        <th>gender</th>
                        <th>birth date</th>
                        <th>action</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(el => { %>
                    <tr>
                        <td><%= el.id %> </td>
                        <td><%= el.first_name %> </td>
                        <td><%= el.last_name %> </td>
                        <td><%= el.email %> </td>
                        <td><%= el.gender %> </td>
                        <td><%= el.birth_date %> </td>
                        <td>
                            <a href="/students/<%= el.id %>/edit"><button
                                    class="btn btn-outline-primary">Edit</button></a>
                            <a href="/students/<%= el.id %>/delete"><button class="btn btn-danger">Delete</button></a>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <% } %>
        </div>
    </div>
</body>

</html>