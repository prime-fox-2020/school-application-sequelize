<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>student by id page</title>
</head>
<style>
    a {
        text-decoration: none;
    }
</style>
<body>
    <nav>
        <ul>
            <li><a href="/students">Data Students</a></li>
            <li><a href="/teachers">Data Teachers</a></li>
            <li><a href="/subjects">Data Subjects (belum ada data hanya judul halamannya saja)</a></li>
            <li><a href="/">Home</a></li>
        </ul>
    </nav>
    <h1>DATA STUDENT BY ID</h1>

    <% if (pesan) {%>
        <h4><%=pesan%></h4>
    <%}%>

    <%if (dataById) {%>
        <table border="2">
            <thead>
                <tr>
                    <td>ID</td>
                    <td>FIRST NAME</td>
                    <td>LAST NAME</td>
                    <td>EMAIL</td>
                    <td>GENDER</td>
                    <td>BIRTH DATE</td>
                    <td colspan="2">OPTIONS</td>
                </tr>
            </thead>
            <tbody>
                <%for (let i in dataById) {%>
                    <tr>
                        <td><%=dataById[i].id%></td>
                        <td><%=dataById[i].first_name%></td>
                        <td><%=dataById[i].last_name%></td>
                        <td><%=dataById[i].email%></td>
                        <td><%=dataById[i].gender%></td>
                        <td><%=dataById[i].birth_date%></td>
                        <td><a href="/students/edit/<%=dataById[i].id%>">EDIT</a></td>
                        <td><a href="/students/delete/<%=dataById[i].id%>">DELETE</a></td>
                    </tr>    
                <%}%>
            </tbody>
        </table>
    <%}%>

</body>
</html>